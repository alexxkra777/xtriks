<script>
  import { updateArray } from '$lib/stores/arrayStore';

  // Example array data
  let arrayData = ["item1", "item2", "item3"];

  // Update the store with the array data
  updateArray(arrayData);
</script>